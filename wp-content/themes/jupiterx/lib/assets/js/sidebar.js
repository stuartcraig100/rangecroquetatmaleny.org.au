/**
 * Refactor the codes to follow same convention as header.
 */

jQuery(document).ready(function ($) {

  var elements = $('.jupiterx-sticky-sidebar .jupiterx-sidebar');
  Stickyfill.add(elements);

});
